clc;
M=50;
N=100;
f0=3;
f1=5;
X=zeros([M N]);
m=0:M-1;
n=0:N-1;
Avg=zeros([50 100]);
for m=0:M-1
    X(m+1, :)=cos(2*pi*(f0*m/M+f1*n/N));
    Avg(m+1, :)=X;
end


for m=0:M-1
    
end
%plot (X);
plot (Avg);
